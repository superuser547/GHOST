# AGENTS.md

## 🧠 Язык взаимодействия

Пожалуйста, отвечай на **русском языке** при анализе, объяснении и генерации кода для этого репозитория.  
Все комментарии, описания и пояснения также должны быть написаны **на русском**, если иное явно не указано в задаче.

## ℹ️ Назначение

Данный файл предназначен для того, чтобы Codex и другие агенты системы корректно определяли язык взаимодействия (русский)  
и сохраняли архитектуру проекта без изменений.  

## ⚙️ Правила для агентов

**Язык:**  
- Все ответы, комментарии, описания и выводы — на русском языке.

## ✅ Обязательные проверки перед коммитом

Перед тем как создавать коммит, агент **обязан** прогнать проверки и убедиться, что они проходят без ошибок.
Коммитить код с проваленными проверками **нельзя**.

### Backend (Python)

Запускать из корня репозитория:

```bash
cd backend
python -m venv .venv 2>/dev/null || true
source .venv/bin/activate
python -m pip install -U pip setuptools wheel
python -m pip install -r requirements.txt
python -m pip install -r requirements-dev.txt

# 1) Линтер (ruff)
ruff check .

# 2) Форматирование (black)
black --check .

# 3) Тесты
pytest -q

deactivate
cd ..
```

Если `ruff check .` падает и предлагает исправить ошибки, то можно автоматически исправить:

```bash
cd backend
source .venv/bin/activate
ruff check . --fix
deactivate
cd ..
```

### Frontend (Node/Vite)

Запускать из корня репозитория:

```bash
cd frontend

# Установка зависимостей
if [ -f package-lock.json ]; then
  npm ci --no-audit --no-fund
else
  npm install --no-audit --no-fund
fi

# 1) Линтер
npm run lint

# 2) Сборка
npm run build

# 3) Проверка форматирования (если есть скрипт format:check)
npm run format:check

cd ..
```

Если `format:check` падает — исправить форматирование:

```bash
cd frontend
npm run format
cd ..
```

## 🧾 Правило коммита

Коммит разрешён только если:
1) `ruff check .` проходит без ошибок (или ошибки исправлены `--fix` и повторно проверено),
2) `black --check .` проходит,
3) `pytest -q` проходит,
4) `npm run lint` проходит,
5) `npm run build` проходит,
6) `npm run format:check` проходит (или выполнен `npm run format`, затем повторная проверка).


## 💬 Пример

**Вопрос:** Где находятся основные требования к проекту?  
**Ожидаемый ответ:**  
Основные требования к проекту находятся в файле `REQUIREMENTS.md`.
